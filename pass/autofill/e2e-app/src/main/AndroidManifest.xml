<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="me.proton.pass.autofill.service" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="proton.android.pass.autofill.e2e.E2EApp"
        android:icon="@mipmap/ic_launcher"
        android:label="E2E Autofill"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat"
        tools:replace="android:theme">
        <activity
            android:name="me.proton.pass.autofill.e2e.proton.android.pass.autofill.e2e.E2EActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
