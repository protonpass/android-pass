<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.proton.android.pass">

    <uses-sdk tools:overrideLibrary="me.proton.core.pass.autofill.service" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="me.proton.android.pass.App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ProtonTheme"
        tools:replace="android:theme">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="me.proton.android.pass.initializer.WorkManagerInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.android.pass.initializer.AccountStateHandlerInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.android.pass.initializer.StrictModeInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.android.pass.initializer.LoggerInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.android.pass.initializer.SentryInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.android.pass.initializer.EventManagerInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="me.proton.core.crypto.validator.presentation.init.CryptoValidatorInitializer"
                android:value="androidx.startup" />
        </provider>

        <activity
            android:name="me.proton.android.pass.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/ProtonTheme.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Autofill -->
        <activity android:name=".ui.autofill.save.AutofillSaveSecretActivity"
            android:theme="@android:style/Theme.Material.Light.Dialog.Alert"
            android:launchMode="singleTop"
            android:clearTaskOnLaunch="true"
            android:excludeFromRecents="true" />
        <activity android:name=".ui.autofill.search.AutofillListSecretsActivity"
            android:theme="@android:style/Theme.Material.Light.Dialog.Alert"
            android:launchMode="singleTop"
            android:clearTaskOnLaunch="true"
            android:excludeFromRecents="true" />
        <!-- /Autofill -->

        <!-- Core-Auth activities -->
        <activity
            android:name="me.proton.core.auth.presentation.ui.AuthHelpActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.ChooseAddressActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.CreateAddressActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.AddAccountActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ProtonTheme.AddAccount"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.LoginActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.signup.SignupActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.TwoPassModeActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.auth.presentation.ui.SecondFactorActivity"
            android:theme="@style/ProtonTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.payment.presentation.ui.BillingActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.payment.presentation.ui.PaymentOptionsActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="me.proton.core.payment.presentation.ui.PaymentTokenApprovalActivity"
            android:windowSoftInputMode="adjustResize" />
        <!-- /Core-Auth activities -->

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

    </application>

</manifest>
